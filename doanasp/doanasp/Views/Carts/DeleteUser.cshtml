@model doanasp.Models.Cart

@{
    Layout = "_LayoutMember";
    string username = "anonymous";
    int Id = 0;
    if (ViewBag.id != null)
    {
        Id = ViewBag.id;

    }
    if (ViewBag.UserName != null)
    {
        username = ViewBag.UserName;
        Id = ViewBag.id;
        @section Member{<li><a asp-controller="Accounts" asp-action="Logout">Logout</a></li>};
        @section name{<li><a asp-controller="Accounts" asp-action="iaccount">@username</a></li> }
    }
    else
    {
        @section Member{};
        @section name{@username}
    }

}
@section idWish{@Id}
@section id{@Id}


<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Cart</h4>
    <hr />
    <dl class="row" style=" margin-left: 5%">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Account.Fullname)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Account.Fullname)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Product.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Product.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Product.SKU)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Product.SKU)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Product.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Product.Price)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Quantity)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Quantity)
        </dd>
    </dl>

    <form asp-action="DeleteUser">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="CartUser">Back to List</a>
    </form>
</div>